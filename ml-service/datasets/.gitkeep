# This file keeps the datasets/ directory tracked by Git.
# Actual dataset CSV files are gitignored (too large for version control).
# Run `python train.py` or your data download script to populate this folder.
